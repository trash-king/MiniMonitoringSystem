#include <vector>
//this contains data related to the "environment". Mostly the master list of all devices on the network.
bool isStreamActive = false;

std::vector<int> transmissionBuffer; //all "messages" are sent to the transmission buffer.

